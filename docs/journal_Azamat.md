Таблица clients (50 400 строк, 8 колонок)
1. Что является строкой в таблице?
Строка = уникальный клиент. Это подтверждается тем, что колонка client_id содержит ровно 50 400 уникальных значений, что равно общему количеству строк в таблице.
2. Какой ключ связывает таблицы, какая доля клиентов "склеивается"?
Ключ связывания: client_id. 
Доля "склеивания": 100% — все 50 400 клиентов из таблицы clients присутствуют в таблице usage (в usage также 50 400 уникальных client_id).
3. За какой период есть данные, есть ли дыры в месяцах?
В таблице clients есть колонки connection_date (58 уникальных значений) и termination_date (27 уникальных значений). Точный период подключения и расторжения можно определить после преобразования дат, но данные охватывают несколько лет работы компании.
4. Как в данных видно отток?
Отток определяется по колонке termination_date:
Если дата заполнена → клиент расторг договор (ушёл)
Если дата пустая (36 361 пропуск, 72.1%) → клиент активен

Мы создали бинарный флаг is_churned на основе этой логики.
5. Сколько клиентов в каждом сегменте и по каждому продукту?

По сегментам:
![alt text](image.png)

По продуктам:
![alt text](image-1.png)

По регионам:
![alt text](image-2.png)

6. Есть ли дубликаты?
Полных дубликатов строк: 0
Дубликатов по client_id: 0
Данные чистые, каждый клиент представлен ровно одной строкой.

7. Есть ли аномалии?

Пропуски: Колонка termination_date имеет 36 361 пропуск (72.1%), но это не аномалия, а нормальная ситуация — у активных клиентов даты расторжения нет.
Отрицательные значения: Не проверялись явно, но данные выглядят корректными.
Выбросы: Не применимо для категориальных данных.

Таблица usage (1 073 079 строк, 7 колонок)

1. Что является строкой в таблице?
Строка = потребление конкретного клиента за конкретный месяц. Это подтверждается тем, что таблица содержит 50 400 уникальных клиентов × 30 месяцев = 1 512 000 возможных комбинаций, но фактически 1 073 079 строк (некоторые клиенты подключились позже или данные неполные).

2. Какой ключ связывает таблицы, какая доля клиентов "склеивается"?
Ключ связывания: client_id + month (составной ключ).
Доля "склеивания": 100% — все 50 400 клиентов из clients присутствуют в usage.

3. За какой период есть данные, есть ли дыры в месяцах?
Колонка month содержит 30 уникальных значений — данные охватывают 30 месяцев. Дыр в месяцах не обнаружено (все 30 месяцев представлены).

4. Как в данных видно отток?
Отток виден косвенно — через связь с таблицей clients по client_id. Если клиент есть в clients с заполненной termination_date, значит он ушёл.

5. Сколько клиентов в каждом сегменте и по каждому продукту?
Эта информация находится в таблице clients, а не в usage. Таблица usage содержит только данные о потреблении.

6. Есть ли дубликаты?
Полных дубликатов строк: 0
Дубликатов по составному ключу (client_id + month): 0
Данные чистые, каждый клиент имеет ровно одну запись за каждый месяц.

7. Есть ли аномалии?
Выбросы в выручке (revenue):
Среднее значение: 195 748.37
Медиана: 36 753.69
Максимум: 10 777 345.63 (в 293 раза больше медианы!)
Минимум: 5.61

Вывод: Среднее в 5 раз больше медианы → есть крупные клиенты-выбросы, которые тянут среднее вверх.